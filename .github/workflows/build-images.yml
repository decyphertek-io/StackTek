name: build images (deprecated)

# DEPRECATED — renamed to stacktek-ghcr-build.yml. This file is kept only as a
# redirect and has its triggers removed so it never fires. Delete it at your
# convenience (it cannot be removed by the edit tooling alone).
#
# The active workflow is:
#   .github/workflows/stacktek-ghcr-build.yml   (stacktek + caddy images)
#   .github/workflows/desktop-ghcr-build.yml    (web desktop images, manual)
#
# ansible-build/.github/workflows/stacktek-build.yml dispatches the
# `stacktek-build` event, which stacktek-ghcr-build.yml now handles.

on:
  workflow_dispatch: {}

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: |
          echo "This workflow is deprecated. See stacktek-ghcr-build.yml."
          echo "Delete this file; it no longer listens on repository_dispatch."
          exit 0
